version: '3.7'

services:
  mysql:
    build:
      context: ./mysql
  joomla:
    build:
      context: ./joomla
  assets-ssh:
    build:
      context: ./assets-ssh
  cms-rproxy:
    build:
      context: ./rproxy
  rproxy-certbot:
    build:
      context: ./rproxy-certbot
